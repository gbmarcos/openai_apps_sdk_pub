name: openai_apps_sdk
description: A Flutter/Dart package that provides seamless integration with OpenAI's Apps SDK for ChatGPT, enabling developers to build interactive widgets and applications that run natively inside ChatGPT conversations.
version: 0.1.0+1

environment:
  sdk: ^3.9.0

dependencies:
  freezed_annotation: ^3.1.0
  html: ^0.15.6
  js: ^0.7.2
  json_annotation: ^4.9.0
  web: ^1.1.1

dev_dependencies:
  build_runner: ^2.9.0
  freezed: ^3.2.3
  json_serializable: ^6.11.1
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
